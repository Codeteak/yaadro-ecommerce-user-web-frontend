/**
 * Optional Next.js client instrumentation hook.
 *
 * Next.js (dev) expects `private-next-instrumentation-client` to be aliased to
 * this file. Keeping this stub avoids runtime "module not found" errors when
 * no client instrumentation is needed.
 */

export function register() {
  // No-op
}

