import HomePageSkeleton from './HomePageSkeleton';

/** Shown via `app/loading.js` when navigating to home — same layout as data-loaded home. */
export default function HomeRouteSkeleton() {
  return <HomePageSkeleton />;
}
